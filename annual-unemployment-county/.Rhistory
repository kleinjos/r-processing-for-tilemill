delinq.df <- read.csv('~/unemployment-j-2012-27-march.csv')
delinq.df <- read.csv('~/unemployment-j-2012-27-march.csv')
delinq.df <- read.csv('/Users/christopherherwig/Documents/MapBox/lee-unemployment/unemployment-j-2012-27-march.csv')
str(delinq.df)
names(delinq.df)
max(delinq.df)
max(delinq.df[, grepl("^X", names(deliq.df))])
max(delinq.df[, grepl("^X", names(delinq.df))])
max.hist<-max(delinq.df[, grepl("^X", names(delinq.df))])
for ( i in names(delinq.df)[grepl("^X", names(delinq.df))]) {
hist(delinq[, i], xlim=c(max.hist))
}
for ( i in names(delinq.df)[grepl("^X", names(delinq.df))]) {
hist(delinq.df[, i], xlim=c(max.hist))
}
for ( i in names(delinq.df)[grepl("^X", names(delinq.df))]) {
hist(delinq.df[, i], xlim=c(0, max.hist))
}
for ( i in names(delinq.df)[grepl("^X", names(delinq.df))]) {
hist(delinq.df[, i], xlim=c(0, max.hist))
}
names(delinq.df)[grepl("^X", names(delinq.df))]
for ( i in names(delinq.df)[grepl("^X", names(delinq.df))]) {
hist(delinq.df[, i], xlim=c(0, max.hist), main=i)
}
image(delinq.df[1:100, grepl("^X", names(delinq.df))])
image(as.matrix(delinq.df[1:100, grepl("^X", names(delinq.df))]))
rossler<-function(n=2000,a=.2,b=.2,c=1.7,x0=0.0001,y0=0.0001,z0=0.0001){
x<-c(x0,rep(NA,n-1))
y<-c(y0,rep(NA,n-1))
z<-c(z0,rep(NA,n-1))
h<-0.015
for (i in 2:n){
x[i]<-x[i-1]-h*(y[i-1]+z[i-1])
y[i]<-y[i-1]+h*(x[i-1]+a*y[i-1])
z[i]<-z[i-1]+h*(b+z[i-1]*(x[i-1]-c))
}
require(rgl)
rgl.clear()
rgl.points(x,y,z, color=heat.colors(n), size=1)
}
rossler(2000,x0=3,y0=4,z0=.4)
install.packages("rgl")
rossler<-function(n=2000,a=.2,b=.2,c=1.7,x0=0.0001,y0=0.0001,z0=0.0001){
x<-c(x0,rep(NA,n-1))
y<-c(y0,rep(NA,n-1))
z<-c(z0,rep(NA,n-1))
h<-0.015
for (i in 2:n){
x[i]<-x[i-1]-h*(y[i-1]+z[i-1])
y[i]<-y[i-1]+h*(x[i-1]+a*y[i-1])
z[i]<-z[i-1]+h*(b+z[i-1]*(x[i-1]-c))
}
require(rgl)
rgl.clear()
rgl.points(x,y,z, color=heat.colors(n), size=1)
}
rossler(2000,x0=3,y0=4,z0=.4)
delinq.df <- read.csv('/Users/christopherherwig/Documents/MapBox/lee-unemployment/unemployment-j-2012-27-march.csv')
str(delinq.df)
names(delinq.df)
max.hist<-max(delinq.df[, grepl("^X", names(delinq.df))])
for ( i in names(delinq.df)[grepl("^X", names(delinq.df))]) {
hist(delinq.df[, i], xlim=c(0, max.hist), main=i)
}
image(as.matrix(delinq.df[1:100, grepl("^X", names(delinq.df))]))
